{
  "name": "socialecho-zapier-integration",
  "version": "1.1.0",
  "description": "Zapier CLI integration for SocialEcho OpenAPI",
  "main": "index.js",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "check": "node --check index.js && node --check authentication.js && node --check creates.js && node --check utils/socialecho.js",
    "validate": "zapier validate"
  },
  "dependencies": {
    "zapier-platform-core": "17.9.1"
  },
  "devDependencies": {
    "zapier-platform-cli": "17.7.2"
  }
}
